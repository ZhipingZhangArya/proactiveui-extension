# Standup — Zhiping — 2026-04-06

**Yesterday**

- Decided the Next.js migration path (Option A: rebuild product, reuse
  pure logic from the VS Code extension).
- Scaffolded Next.js 15 + Tailwind; landing page rendered.
- Moved the VS Code extension to `legacy/` with `git mv` (history
  preserved).

**Today**

- Add Clerk auth + Prisma schema for User / Document / Agent.
- Dashboard shell with Monaco editor for Python + LaTeX.

**Blockers**

- None. Vercel Postgres provisioning can wait — using a placeholder
  `DATABASE_URL` for local dev and I already thought through a
  `PROACTIVEUI_DEV_BYPASS_AUTH` flag for demoing without the DB.

**Notes for Anya**

- Skills directory shape in `.claude/skills/<name>/SKILL.md` matches
  the rest of the course. Your part1 add-intent skill is already in
  that shape — no changes needed on your side.
