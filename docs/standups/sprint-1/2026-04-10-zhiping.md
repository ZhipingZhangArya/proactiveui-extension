# Standup — Zhiping — 2026-04-10

**Yesterday**

- Swapped Clerk for Auth.js Credentials (username + password, no
  OAuth, no forgot-password). Simpler, fits the HW auth requirement,
  and users don't need to sign up for a 3rd-party service to try the
  app.
- bcrypt hashing + zod validation on signup + signin.
- Updated Prisma User model: `username` unique + `password` hash.

**Today**

- Port the pure-logic modules (`SessionStats`, `mockIntentAnalyzer`,
  `AnthropicIntentClient`) from `legacy/` into `src/lib/`.
- Re-run the 24 legacy SessionStats tests in the new setup.

**Blockers**

- None. Next thing that needs external action is Vercel Postgres
  provisioning — tracking that as an S17 blocker for Sprint 2.

**Notes for Anya**

- The `vscode.Range` → `TextRange` rename is the only non-trivial
  edit during the port — everything else copies as-is. No change
  needed for your part1 work.
